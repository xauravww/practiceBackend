<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=
    , initial-scale=1.0"
    />
    <title>Profile Page</title>
  </head>
  <body>
    <%- include('common/header')%> Welcome <%= user.name%>
    <h3><%= user.email%></h3>
    <h3><%= user.city%></h3>

    <ul>
      <% user.skills.forEach((item)=>{%>
      <li><%= item %></li>
      <%})%>
    </ul>
  </body>
</html>
